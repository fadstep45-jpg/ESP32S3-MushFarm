// Shim: pull mf_log.cpp into this test sketch as its own translation unit.
// See test_README.md for the rationale (per-file shim avoids static name
// collisions that would happen with a single combined include).
#include "../mushfarm/mf_log.cpp"
