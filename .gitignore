# Arduino IDE / arduino-cli build output
/build/
/build-*/
firmware/mushfarm/build/
firmware/build/
**/*.bin
**/*.elf
**/*.map

# Arduino IDE local backups / merged sketch
firmware/mushfarm/mushfarm.ino.cpp
firmware/mushfarm/mushfarm.ino.merged.cpp
firmware/mushfarm/mushfarm.ino.partitions.csv

# Legacy ESP-IDF tree is kept locally only — never tracked in the repo.
legacy_esp_idf/

# OS / IDE
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
